bool isPrime(int numberToCheck){    //return false if one or true because they are not primeif (numberToCheck == 0 || numberToCheck == 1) {        return false;    }    else {        //we loop through every number before the number to check        for (int i = 2; i <= numberToCheck / 2; ++i) {            // if something divides evenly we return falsee            if (numberToCheck % i == 0) {                return false;            }        }    }    // if it does return false by here the number is prime    return true;}int main(){    return 0;}